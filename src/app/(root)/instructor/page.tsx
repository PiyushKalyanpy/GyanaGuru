import { Button } from "@/components/ui/button";

const Instructor = () => {
    return (
        <div>
            <Button >Create Course </Button>
        </div>
    );
}

export default Instructor;